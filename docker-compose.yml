version: "3.9"

services:
  postgres:
    image: davinci-postgre-image
    container_name: davinci-postgre
    ports:
      - "5432:5432"

  gui:
    image: davinci-gui-image
    container_name: davinci-project-server-gui
    ports:
      - "8090:8090"
    depends_on:
      - postgres
